# Pehchan Karo Frontend

The frontend for **Pahechan Karo**.

## 🛠️ Tech Stack

- **Framework**: Next.js 15 is6753ds
- **Language**: TypeScriptee
- **Styling**: Tailwind CSS 89xx
- **Icons**: Lucide Reacts
- **Utilities**:
    - `clsx` for conditional class handling
    - `class-variance-authority` for variant-based class management
    - `tailwind-merge` for merging Tailwind classes safelyz
    - `tw-animate-css` for reusable animations
    - ssdsaddd

## 📦 Package Manager
ss
This project uses **[pnpm](https://pnpm.io/)** for dependency management. Make sure it's installed:

```bash
npm install -g pnpm
```

## 🚀 Available Scripts

| Command    | Description                       |
| ---------- | --------------------------------- |
| pnpm dev   | Runs the development server       |
| pnpm build | Builds the app for production     |
| pnpm start | Starts the production server      |
| pnpm lint  | Runs ESLint to check code quality |

## 🗂 Project Structure

```
pehchankaro-fe/
├── app/               # (or pages/) - Next.js routing
├── components/        # Reusable UI components
├── lib/               # Utility and helper functions
├── styles/            # Global styles and Tailwind config
├── public/            # Static assets
├── tsconfig.json      # TypeScript configuration
├── tailwind.config.ts # Tailwind configuration
├── next.config.js     # Next.js configuration
├── package.json       # Project manifest
├── pnpm-lock.yaml     # Lock file for reproducible installs
└── README.md # You're reading it
```
